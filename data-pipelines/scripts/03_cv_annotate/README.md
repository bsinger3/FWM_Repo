# 03 CV Annotate

Scripts that assign stable `review_id` values, add image-level CV annotations,
generate crop metadata, and build review packages belong here.

- `amazon/build_supabase_image_review_package.py`: builds the current Stage 03
  review package. The old Amazon Step 4 path is a compatibility wrapper.
